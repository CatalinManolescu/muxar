{
  "name": "muxar",
  "private": true,
  "version": "0.0.1",
  "description": "muxar",
  "repository": "https://github.com/catalin-/muxar",
  "license": "Apache License, Version 2.0",
  "devDependencies": {
    "http-server": "^0.8.5"
  },
  "dependencies": {
    "restify": "^4.0.3"
  },
  "engines": {
    "node": ">=4.2.6"
  },
  "config": {
    "port": 80,
    "path": {
      "public": "../"
    }
  },
  "scripts": {
    "postinstall": "",
    "prestart": "npm install",
    "start-web": "http-server -a localhost -p 80 -c-1 ./web/public",
    "start": "node server/app.js"
  }
}
